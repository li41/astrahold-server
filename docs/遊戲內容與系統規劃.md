# 星壘 — 遊戲內容與系統規劃

本文件只記錄**已討論確認的內容**、仍待討論的設計空缺，以及後續 Server gameplay roadmap。

任何武器、怪物、技能、掉落、商店、數值、經濟、取得方式，都不能因為「系統已經能做」就由 agent 自行補完。

## 一、內容設計原則

1. 先和使用者討論內容，再實作。
2. Server correctness 可以直接修，但內容數值與取得方式不得自行決定。
3. Gameplay feature 先做通用 module，再透過 archetype / stats / policy / loot table / AI config / respawn policy 擴充。
4. 不為單一怪物、單一 item、單一 map 複製 gameplay system。
5. Client 不決定 damage、loot、equipment legality、AI、HP/MP、cooldown 或 persistence outcome。

## 二、第一批低階裝備 v1

### 民兵鐵劍

- ID：`item_militia_iron_sword`
- 小型傷害：6–9
- 大型傷害：6–8
- 額外傷害：0
- 命中修正：0
- 基本攻擊間隔：1.00 秒
- 重量：7
- 材質：鐵

### 輕衛鐵劍

- ID：`item_light_guard_sword`
- 小型傷害：5–8
- 大型傷害：5–7
- 額外傷害：0
- 命中修正：+2
- 基本攻擊間隔：0.85 秒
- 重量：6
- 材質：鐵

### 鬥士鐵劍

- ID：`item_gladiator_iron_sword`
- 小型傷害：7–10
- 大型傷害：6–9
- 額外傷害：0
- 命中修正：0
- 基本攻擊間隔：1.10 秒
- 重量：8
- 材質：鐵

### 民兵戰斧

- ID：`item_militia_battle_axe`
- 小型傷害：5–8
- 大型傷害：8–12
- 額外傷害：0
- 命中修正：-1
- 基本攻擊間隔：1.15 秒
- 重量：10
- 材質：鐵／木柄

### 鐵頭戰錘

- ID：`item_iron_war_mace`
- 小型傷害：6–9
- 大型傷害：6–9
- 額外傷害：+1
- 命中修正：+1
- 基本攻擊間隔：1.15 秒
- 重量：9
- 材質：鐵／木柄

## 三、第一批低階盾牌 v1

### 木鐵圓盾

- ID：`item_iron_rim_round_shield`
- 物理防禦：+3
- 格擋率：8%
- 格擋成功減傷：25%
- 魔法減傷：0%
- 重量：6

### 守備盾

- ID：`item_guard_shield`
- 物理防禦：+4
- 格擋率：10%
- 格擋成功減傷：30%
- 魔法減傷：0%
- 重量：8

### 方紋魔盾

- ID：`item_runed_square_shield`
- 物理防禦：+2
- 格擋率：6%
- 格擋成功減傷：20%
- 魔法減傷：8%
- 重量：6
- 定位：低階魔法盾

## 四、低階裝備取得方式

目前 8 件低階裝備**完全沒有正式取得方式**。

沒有：

- 怪物掉落
- 掉率
- NPC 商店
- 狼皮兌換
- 任務
- 製作
- 初始裝備
- 金幣價格

取得來源與經濟設計之後再逐項討論。

## 五、武器目前 Server 規則

PR #149 已實作：

- 只有 entity-target `basic-attack` 使用本批武器的 authored damage。
- Server 先驗證 target / range / LOS / resource / protection legality，再擲傷害。
- Client 不提供傷害值或隨機 roll。
- CombatEvent、HP mutation、loot contribution 使用同一份 Server actual damage truth。
- attack interval 沿用既有 combat cooldown authority，不新增第二套 cooldown system。
- siege / gate、skills、AI special action 不得因這批武器公式被意外改變。

### 命中修正

`accuracy_modifier` 已有資料，但**公式尚未討論，所以目前不生效**。

## 六、盾牌格擋目前已確認規則

### Authoritative flow

```text
Server 收到合法傷害
-> 確認這次傷害能不能被盾牌格擋
-> Server 擲 BlockChance
-> 成功才套 BlockReduction
-> 產生最終 authoritative damage
-> 更新 HP / CombatEvent
-> Client 只做呈現
```

### 已鎖定

- `BlockChance` 只由 Server 擲。
- Client 不能回報「我格擋成功」。
- `BlockReduction` 只有格擋成功才套用。
- 同一筆傷害只做一次格擋 roll。
- 最終 HP mutation、CombatEvent、傷害數字使用同一份最終 Server damage。
- 格擋動畫、VFX、音效只能根據 Server 結果播放。
- v1 不看角色面向、不看攻擊角度、不做背後繞盾判定。

### 職業限制

低階盾：**所有職業都能使用**。

較高品質盾牌未來可以限制職業，但實際職業分類與名單尚未決定。

裝備資格由 Server 驗證；Client 只能做 UI 提示。

建議資料模型採明確 Equipment Policy，例如：

```text
ClassPolicy = All
```

或未來：

```text
ClassPolicy = AllowList
AllowedClassIDs = [...stable ClassID...]
```

不要在 combat code 寫死「低品質必定全職業」。Item archetype 應明確表達資格。

### 仍待討論

以下都還不能自行實作：

- `PhysicalDefense +2/+3/+4` 如何換算成最終減傷。
- 格擋是否只適用 entity-target physical basic attack，或其他 ranged / skill / AI action 也可以 block。
- magic reduction 的正式 damage type contract。
- 最終 damage rounding。
- minimum damage 是否固定至少 1。
- Server event 要用什麼欄位告知 Client「本次 block 成功」。

先前提出過的 `PhysicalDefense / (PhysicalDefense + 20)` 只是候選公式，**尚未正式定案**。

## 七、怪物體型

Server `EntityState.BodySize` 是目前 authoritative body-size truth。

規則：

- 不從 Client scale 推測。
- 不從舊 entity incarnation 推測。
- 尚未討論的 monster archetype 不正式宣告 small / large。
- 未分類 monster 目前走 small compatibility damage table，只是 compatibility 行為，不代表內容上已定義成小型。

## 八、吸魔武器

PR #148 是技術原型，不是正式內容。

### 已撤回

- 固定 `+5 MP`
- `3 灰狼皮 -> 1 法杖`
- 任何因原型而自動成立的正式商店／掉落／兌換

### 已確認方向

- 吸取量不能固定 +5。
- 不能對同一目標無限吸。
- 應與怪物／目標的 MP 或可吸額度相關。
- 吸取數值應有 Server 決定的隨機變化。
- 必須建立在 Server-confirmed actual damage / legal hit 之後。

完整公式與正式內容日後再討論。

## 九、後續 combat / encounter 系統

### 單一 incoming-damage pipeline

目標是讓所有會傷害 entity 的路徑共享同一個 defensive resolution，而不是 melee、ranged、monster、skill 各寫一份。

建議順序：

```text
合法傷害來源
-> DamageType / Blockable 等 Server semantics
-> 防禦 / resistance
-> block roll（若可格擋）
-> proc / status（依正式順序）
-> clamp / rounding
-> final damage
-> HP mutation
-> CombatEvent / contribution / presentation event
```

正式順序在實作前仍需鎖定。

### Threat table

從目前 retaliation 逐步升級：

- stable `EntityID` / character identity
- actual damage contribution
- target invalidation / fallback
- death / disconnect cleanup
- evade / encounter reset cleanup
- pack assist 走 AI policy

Threat 與 loot contribution 可以共用「Server 確認的實際傷害事實」，但兩者不能共用同一份 gameplay table。

### Status / buff / debuff

未來 poison、slow、stun、temporary shield、weapon enchant、resistance 等應走通用 effect lifecycle：

- stable effect ID
- source / target
- authoritative start / end tick
- stack / refresh policy
- apply / revert 成對
- persistence policy

### Monster abilities

做通用能力系統後才逐隻怪物配置：

- cast range / LOS
- cooldown
- target selector
- HP threshold
- direct / area damage
- heal
- buff / debuff
- poison
- summon

具體 monster / skill 內容必須先討論。

## 十、物品、經濟與多人內容 roadmap

### Item instance

未來若加入：

- durability
- enchant
- binding
- tradable state
- instance roll

則 drop / pickup / persistence / warehouse / trade 全部必須完整保留同一 item instance state。

### Economy transaction

- shop preflight / atomicity
- warehouse integrity
- player trade escrow
- confirmation reset
- cancel restore
- crash recovery
- idempotency

### Party

- invite / accept / leave / leader
- stable member identity
- vitals presentation
- loot policy
- cooperative threat semantics

### Quest

以 Server authoritative event 更新 objective：

- kill
- pickup
- interact
- enter-area

reward 不可因 reconnect / retry 重複發放。

## 十一、參考 `myriad-throne-server` 的原則

可參考的是成熟 MMORPG 的 invariant 與 regression tests，不是其內容或舊架構。

值得借鑑：

- 共用 damage resolution
- equipment derived state rebuild
- status apply / revert lifecycle
- threat consistency
- transaction rollback / failure tests
- persistence snapshot / journal correctness
- action queue reentrancy
- visibility / AOI isolation

禁止照搬：

- 具體武器／技能／職業／怪物／掉落
- L1 protocol / compatibility
- Client gfx ID
- session identity 當 gameplay identity
- 為了參考專案而改成 Lua-first gameplay architecture
