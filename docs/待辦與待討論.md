# 星壘 Astrahold — 待辦與待討論

本文件整理目前 **gpt-server 可直接施工的待辦、等待 Client 的整合、需要產品決策的議題，以及明確暫不由 Server 規劃的範圍**。

本文件不是第二份 gameplay contract。若內容與正式 Server source／Protocol contract 衝突，以正式 source／contract 為準；exact SHA、CI、active Issue 與 blocker 以 [專案狀態](專案狀態.md) 為準；長期方向以 [規劃總覽](規劃總覽.md) 為準。

## 工作分類原則

- **可直接做**：不需要新增產品語義，且能以現有 Server authority／contract 判定正確性。
- **等待 Client**：Server contract 已存在，剩餘工作由正式 Client 消費、呈現或回報 integration requirement。
- **待討論**：會新增 gameplay、成長、經濟、取得方式、數值或新 Protocol semantics；沒有明確決策前 Server 不猜。
- **暫不規劃**：目前不是 Server-first 工作；不以 infrastructure-first／framework-first／content-churn 填空。

## P0 — Server correctness／contract 收斂

Protocol v29 七欄裝備與完整 45 件防具已落地。Server 目前沒有已確認需要另開新 Protocol 的 armor blocker。

防具 correctness 要求：

- 正式七欄：`main_hand`、`off_hand`、`helmet`、`chest`、`gloves`、`legs`、`boots`。
- 45 件防具：15 低階、15 中階、15 高階。
- 中階 unique armor：1 條 Server-rolled affix。
- 高階 unique armor：2 條不同 Server-rolled affix。
- 星鑄壁壘／星影追獵／星輝法衣的 requirement 分別只看 durable base Constitution／Agility／Intelligence >= 18。
- live equip 與 durable restore 都必須重新驗 requirement；裝備、詞綴、set bonus、passive/status bonus 不得滿足自身 requirement。
- 2/5 件套裝效果由當下 authoritative equipped pieces 重算，不保存第二份 set-active state。

本輪 exact-head validation 與 Client handoff 狀態見 [專案狀態](專案狀態.md)。

### 下一個直接施工項目

**Map1 monster content model + Emberwatch Fields / Witherwill 第一批正式怪物。**

正式規劃見 [Map1-怪物規劃](Map1-怪物規劃.md)。10 種怪物的 authored catalog 與普通近戰 foundation 已由 Server `7c401574...` 實作，CI run `35340474912` 的 Test／Vet／race detector PASS：包含 `DamageMin..DamageMax` Server roll、monster hit/evasion/critical/physical+magic defense、`defense/(defense+100)`、per-archetype attack interval/range、AggroMode、threat、same-family one-hop assist、corpse/respawn seam；playtest 灰狼已使用 `monster_gray_wolf` + shared `monster-melee`。Map1 terrain 半成品已確認存在於 Assets candidate bake；Server `94dbe472...` / CI `35350154037` 已完成通用 heightfield consumption seam。下一步不是再造 combat 或重畫地圖，而是 promotion 真 `land.bin`／accepted collision proposal 到正式 Server world bundle，然後依實際 terrain/roads 落 Emberwatch Fields／Witherwill／Redsoil／South Coast exact spawn/home/patrol/encounter groups。玩家 HP/MP progression、XP owner 仍為獨立待辦；特殊攻擊明確延後另行規劃。Loot Catalog V2 已完成，不再列 monster combat blocker。

怪物掉落 V1 已由 Server `07c60110ab1d6d94e176bc0148c965bd9b6a514e` 實作並由 CI run `35347470862` **PASS**：10 種怪物 exact table、`item_gold_coin` quantity、箭 bundle、low-tier stack equipment、TierMid exact ItemInstance + 單次 affix roll、auto-loot／public pickup payload continuity 都已進 authoritative runtime；Protocol 維持 v34。下一步是 Map1 spawn placement 完成後做真 runtime economy/balance pass，不再重造 loot authority。

小型加速藥水 `item_minor_speed_potion` 的 Map1 掉落來源與機率已定；移速增益百分比、持續時間、cooldown group 與 speed-modifier stacking rule 仍待通用 item-use／movement-status slice 定案與驗證。

## 等待正式 Client 回饋／消費

### Client #21／#22／#24／#25／#26／#27／#29／#34 — 已完成

- Client #21：classless runtime retirement 已完成。
- Client #34：active Protocol v34 cutover 已完成；Client `0b6fbf1e...` 的 client-ci `35312797404` 與 dedicated BrowserWS live job `105498292274` PASS，已驗 v34 handshake、Type118 `flaw` build→clear、retired 9／115／116／117 absence，Issue 已關閉。
- Client #22：Type118 BrowserWS live authority checkpoint 已完成。
- Client #24：Type119–121 exact unique equipment current-v33 BrowserWS runtime acceptance 已完成並關閉。
- Client #25：Type122 AppearanceSnapshot 的 decode/store、玩家可見 selected SkinID／affinity presentation、toggle 與 durable relogin restore 已完成並關閉。
- Client #26：current-v33 已完成 45 件 armor identity、前七欄 UI、Type3／Type119 armor exact-slot 操作、rejection no-optimistic mutation、Server affix presentation 與 reconnect/relogin Type111/120/121 authoritative restore；client-ci `35309579844` 與 dedicated live job `105488859854` PASS，Issue 已關閉。
- Client #27：Protocol v33 弓／十字弓彈藥、雙擊選箭與 live runtime acceptance 已完成並關閉；Server balance-only checkpoint `34fea259...` / CI #1491 將 bow 本體收斂為 2–3／3–4／4–5，最終加箭為 8–11／9–12／10–13，無新 Client 工作。
- Client #29：map0 奧德里克倉庫 current-v33 BrowserWS runtime acceptance 已完成並關閉；Type125–127 authority、GM/non-GM、personal warehouse 與 reconnect restore 已有 Client live 證據。

## 已完成的 Protocol cleanup

Server Protocol v34 已完成 legacy message type 9／115／116／117 breaking removal；current `wrong_class` product semantics亦不存在。Type118 `CharacterTargetResourceState` 保持正式 authority與 ID=118；歷史 persistence `class_id` migration boundary則刻意保留。

## 待產品／使用者決策

### 1. 裝備取得／經濟來源

Map1 怪物掉落的 acquisition direction 已有正式規劃；剩餘產品決策集中在後續 Region／商店／任務／製作／完整經濟，而不是 map1 是否由怪物提供 low-tier / limited mid-tier 裝備。

目前 production catalog 已有 **107 個 equipment archetype**：

- 53 武器
- 9 盾牌
- 45 防具

因此「中高階 item 還不存在」不再是待辦。仍未定的是玩家如何取得：

- 後續 Region 的怪物掉落表／掉率；Map1 exact loot + gold 已有正式規劃
- 商店與價格
- 初始裝備
- 任務獎勵
- 製作／材料兌換
- 交易／經濟價值

沒有正式決策前 Server 不自行補完。

### 2. 角色成長與屬性點 mutation

已定方向：六大基礎屬性、中性基準 10、創角額外 10 點、創角單項最多 +5、51 級後每級 +1 點。

仍需決定正式 character level/progression owner、創角配點 intent、51 級後點數 mutation，以及是否有重置與其成本／限制。

### 3. 技能取得／學習與六格編輯

已有 20 技能 stable ID、learned-skill authoritative state、6 格 combat loadout 與 durable persistence。

仍需決定技能取得／學習、忘卻／重置、六格何時可編輯與 Client intent/rejection semantics。

### 4. 尚未定案的技能效果／數值

至少包含日光術、防禦、治癒術、瞬間移動術，以及 12 個近戰／遠攻／法術技能的正式傷害、距離、冷卻、資源與係數；靈力凝聚與加速術仍有部分數值／作用範圍未定。

### 5. Skin selection mutation

目前已有 durable selected `SkinID` 與 Type122 Server->Client snapshot，但玩家何時／在哪裡切換 skin 的正式產品入口與 legality 尚未定義。

## 地圖／場景 — Client 主導

Client 已 handoff 的正式 World Master authority facts 可由 Server 保存並用於 movement／region／map authority；Server 仍不主動設計下一張 Client 地圖、場景資產、視覺動線或 presentation geometry。

新的 collision／portal／spawn／AI region／siege legality 只有在 Client 提供明確 playable slice 或使用者直接定義需求後才施工。

## P1／P2 — 有正式產品需求再排程

- monster abilities。
- slow／stun／poison／buff／debuff／summon 等 effect content。
- economy transaction、party、quest。
- pet、mail、private shop、auction、crafting、guild、war、siege、ranking、fishing、housing。

每次選下一個 Server slice 仍以：

**玩家可見、可玩的結果 > Server authority > 商用品質 > AI 可維護性 > 可測試性與授權安全**

為優先順序。
