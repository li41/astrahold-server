# 星壘 Astrahold — 待辦與待討論

本文件整理目前 **gpt-server 可直接施工的待辦、等待 Client 的整合、需要產品決策的議題，以及明確暫不由 Server 規劃的範圍**。

本文件不是第二份 gameplay contract。若內容與正式 Server source／Protocol contract 衝突，以正式 source／contract 為準；exact SHA、CI、active Issue 與 blocker 以 [專案狀態](專案狀態.md) 為準；長期方向以 [規劃總覽](規劃總覽.md) 為準。

## 工作分類原則

- **可直接做**：不需要新增產品語義，且能以現有 Server authority／contract 判定正確性。
- **等待 Client**：Server contract 已存在，剩餘工作由正式 Client 消費、呈現或回報 integration requirement。
- **待討論**：會新增 gameplay、成長、經濟、取得方式、數值或新 Protocol semantics；沒有明確決策前 Server 不猜。
- **暫不規劃**：目前不是 Server-first 工作；不以 infrastructure-first／framework-first／content-churn 填空。

## P0 — Server correctness／contract 收斂

Protocol v29 七欄裝備與完整 45 件防具已落地。Server 目前沒有已確認需要另開新 Protocol 的 armor blocker。

防具 correctness 要求：

- 正式七欄：`main_hand`、`off_hand`、`helmet`、`chest`、`gloves`、`legs`、`boots`。
- 45 件防具：15 低階、15 中階、15 高階。
- 中階 unique armor：1 條 Server-rolled affix。
- 高階 unique armor：2 條不同 Server-rolled affix。
- 星鑄壁壘／星影追獵／星輝法衣的 requirement 分別只看 durable base Constitution／Agility／Intelligence >= 18。
- live equip 與 durable restore 都必須重新驗 requirement；裝備、詞綴、set bonus、passive/status bonus 不得滿足自身 requirement。
- 2/5 件套裝效果由當下 authoritative equipped pieces 重算，不保存第二份 set-active state。

本輪 exact-head validation 與 Client handoff 狀態見 [專案狀態](專案狀態.md)。

### 下一個直接施工項目

完成本輪 armor correctness／文件／Client handoff 後，若沒有新的 Client integration blocker，下一個 Server slice 優先從能形成玩家可見／可玩的 loop 選擇。

裝備取得來源目前仍屬產品決策；在使用者定義正式 acquisition semantics 前，不自行發明掉率、商店、價格、任務或製作來源。

## 等待正式 Client 回饋／消費

### Client #29 — Protocol v32 GM 密室奧德里克倉庫（current handshake v33 相容）

狀態：**open；等待 Client 實際 runtime 驗收回報**。

Server 已完成：
- map0 正式 `worlds/gm-room/gameplay.json` / `stock-keeper` authority。
- Type125 `ClientWarehouseCommand` BrowserWS ingress + ownership fence。
- Type126 `WarehouseResult` / Type127 `WarehouseSnapshot` live round-trip。
- Server-only trusted AuthenticationSubject；GM subject 固定由 Server 判定。
- GM open/withdraw、non-GM `not_authorized`、personal open/deposit/withdraw。
- Reliable replay live 驗證不重複發物。
- code checkpoint `7da0d7418a54c9f8a43583b6b77beffffaaace5d`，Server CI #1470 PASS。

Client 尚需回覆：
- map0 玩家可見「奧德里克」位於 `stock-keeper` presentation point。
- 精確台詞與兩個選項。
- `open_gm` / `open_personal` v32 intent。
- Type126 `client_action_sequence` correlation、Type127 replacement snapshot。
- authoritative inventory/warehouse UI 更新，包含 rejection。
- reconnect 後重新以 Server state/snapshot 為準。

Client #29 未回報成功前，不關閉「奧德里克 NPC 玩家可見整合」。


### Client #27 — 弓／十字弓彈藥 V1

狀態：**Server v33 重新施工完成 candidate；NOT VALIDATED，尚未 handoff Client acceptance**。

v33 candidate `78cb04a84892ba8fe43521b55613b73ef27ceae3`：
- bow／crossbow 都需要木箭或銀箭。
- automatic 木箭 -> 銀箭。
- Type128 `ClientAmmunitionCommand{operation:"select"}`。
- Type129 `AmmunitionResult`。
- Type130 `AmmunitionState`，空 selected ID = auto。
- Client 雙擊箭只送 intent；Server 驗 inventory truth 後切換。
- selected stack 耗盡自動回 auto；V1 reconnect 也回 auto。
- crossbow 保留既有 authored damage，但每發成立射擊消耗 1 支實際選中的箭。
- silver arrow 的 undead +20% 同時適用 bow／crossbow。

CI 通過前不得宣稱 PASS，也暫不關閉 Client #27。


### Client #26 — Protocol v29 七欄裝備＋45 件防具

狀態：**open；等待 Client current-v33 consumption / runtime acceptance**。

Server 正式提供：

- Protocol v29 hard fence。
- Type 3／111／119／120／121／122 message ID 不重編。
- 七個固定 equipment slot。
- 45 件 stable armor `ItemArchetypeID`。
- 中高階 exact `ItemInstanceID` equip semantics。
- 中階 1 詞綴／高階 2 不同詞綴。
- 高階 base-stat requirement authority。
- 六套 2/5 件 set bonus authority。
- durable restore/relogin 後 authoritative snapshot 重建。

Client 只消費／呈現 Server state；不得以 UI、asset mapping、local stat calculation 或 optimistic equipment state 決定 gameplay。

### Client #24 — unique-equipment Type119–121

狀態：**open；等待 Client current-v33 consumption / live validation**。

這是較早的 unique-equipment consumer 工作；v29 沿用同一 Type119–121 message identity。Server 不另建 alternate wire。

### Client #25 — AppearanceSnapshot Type122

狀態：**open；等待 Client decode/store/presentation/live validation**。

Server 已正式提供 selected stable `SkinID` 與 `basic_attack_affinity_bonus`。Client 只消費／呈現，不在本地重算 skin-to-WeaponType gameplay match。

### Client #21／#22 — 已完成

- Client #21：classless runtime retirement 已完成。
- Client #22：Type118 BrowserWS live authority checkpoint 已完成。

## 延後的 Protocol cleanup

legacy message type 9／115／116／117、`wrong_class` compatibility vocabulary 等 breaking cleanup，等目前 v33 ammunition／warehouse／equipment 與 Type122 Client consumption 穩定後再獨立規劃。

Type118 `CharacterTargetResourceState` 保持正式 authority；不因 cleanup 移除。

## 待產品／使用者決策

### 1. 裝備取得／經濟來源

目前 production catalog 已有 **107 個 equipment archetype**：

- 53 武器
- 9 盾牌
- 45 防具

因此「中高階 item 還不存在」不再是待辦。仍未定的是玩家如何取得：

- 怪物掉落表與掉率
- 商店與價格
- 初始裝備
- 任務獎勵
- 製作／材料兌換
- 交易／經濟價值

沒有正式決策前 Server 不自行補完。

### 2. 角色成長與屬性點 mutation

已定方向：六大基礎屬性、中性基準 10、創角額外 10 點、創角單項最多 +5、51 級後每級 +1 點。

仍需決定正式 character level/progression owner、創角配點 intent、51 級後點數 mutation，以及是否有重置與其成本／限制。

### 3. 技能取得／學習與六格編輯

已有 20 技能 stable ID、learned-skill authoritative state、6 格 combat loadout 與 durable persistence。

仍需決定技能取得／學習、忘卻／重置、六格何時可編輯與 Client intent/rejection semantics。

### 4. 尚未定案的技能效果／數值

至少包含日光術、防禦、治癒術、瞬間移動術，以及 12 個近戰／遠攻／法術技能的正式傷害、距離、冷卻、資源與係數；靈力凝聚與加速術仍有部分數值／作用範圍未定。

### 5. Skin selection mutation

目前已有 durable selected `SkinID` 與 Type122 Server->Client snapshot，但玩家何時／在哪裡切換 skin 的正式產品入口與 legality 尚未定義。

## 地圖／場景 — Client 主導

Client 已 handoff 的正式 World Master authority facts 可由 Server 保存並用於 movement／region／map authority；Server 仍不主動設計下一張 Client 地圖、場景資產、視覺動線或 presentation geometry。

新的 collision／portal／spawn／AI region／siege legality 只有在 Client 提供明確 playable slice 或使用者直接定義需求後才施工。

## P1／P2 — 有正式產品需求再排程

- monster abilities。
- slow／stun／poison／buff／debuff／summon 等 effect content。
- economy transaction、party、quest。
- pet、mail、private shop、auction、crafting、guild、war、siege、ranking、fishing、housing。

每次選下一個 Server slice 仍以：

**玩家可見、可玩的結果 > Server authority > 商用品質 > AI 可維護性 > 可測試性與授權安全**

為優先順序。
