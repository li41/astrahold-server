# 星壘 Astrahold — 專案狀態

本文件記錄 **目前** 的 Server 施工狀態、Protocol、整合 Issue、blocker 與驗證結果。長期方向見 [規劃總覽](規劃總覽.md)；下一步如何分類見 [待辦與待討論](待辦與待討論.md)。

歷史 milestone／recovery／舊 PR／舊 branch 只作證據，不在本文件重複堆疊完整流水帳。

## 目前基準

- 正式 Server：`li41/astrahold-server`
- gpt-server 唯一工作 branch：`ai-working-gpt-server`
- 正式 `main`：`75468425106965a2a0a86f8c6a56a1fdf282947b`
- 長期 PR／Actions 入口：PR #179
- 現行 Protocol：**v28**
- character schema：**v13**
- save journal schema：**v12**
- 最新已驗證 code checkpoint：`d7d3a02fd94c0bc5e0a64c7ef5f4164d51602c1d`
- 該 exact head 的 PR workflow：**18/18 completed，0 failure**。
- 覆蓋 Server CI、S3-E Scaling 500、S3-E.8 Repeated Churn、S3-E.9 Long Mixed Gameplay Soak，以及 production TLS／trusted-proxy／revocation／recovery E2E。

其後若只有 `docs/**` 文件提交且 workflow 因 `paths-ignore` 未執行，不能把文件 head 宣稱成新的 code PASS；code 驗證仍明確歸屬 `d7d3a02...`。

## 目前最重要的狀態

### 1. Protocol v28 gameplay contract — Server 完成

v28 正式啟用：

- Type119 `ClientEquipmentInstanceCommand`
- Type120 `InventoryInstanceSnapshot`
- Type121 `EquipmentInstanceSnapshot`
- Type122 `AppearanceSnapshot`

正式 v28 gameplay cutover checkpoint：

- `f975d3c8af061336e7f03a91a356d360bfe8577f`
- Server CI #1208 / run `34839603635`
- test／vet／configured race detector PASS

character-owner ReliableOrdered batch：

```text
InventorySnapshot
-> InventoryInstanceSnapshot (120)
-> EquipmentSnapshot
-> EquipmentInstanceSnapshot (121)
-> AppearanceSnapshot (122)
```

Type120／121 是 complete replacement snapshot；空集合也有意義，Client 必須能清 stale unique-instance state。

### 2. 500-client capacity gate 已恢復

先前 `74221a4040...` 曾觀察到 teleport churn burst 超過 `<50ms` hard gate；該狀態已不再是目前 blocker。

最新已驗證 exact head `d7d3a02...`：

- S3-E Scaling 500：PASS
- S3-E.8 Repeated Churn Soak：PASS
- S3-E.9 Long Mixed Gameplay Soak：PASS
- Server CI：PASS

因此目前沒有已確認的 500-client gameplay／network／capacity blocker。先前單次 S3-E.9 出現的 UDP `connection refused` 已在後續 exact-head run 消失，只保留為歷史診斷，不據此改 gameplay semantics。

50ms capacity gate 保持原樣；未來若再回歸，仍以 exact-head workflow 與 artifact 定位，不放寬 threshold。

### 3. Protocol v28 production validation marker 遷移完成

v28 cutover 後曾有多支 production trusted-proxy／TLS／revocation workflow 仍等待或輸出 `protocol=27`，造成正常啟動的 v28 Server 被驗證流程誤判 timeout。

本輪已逐支確認並只遷移過時的 ready／evidence marker；原本 TLS／trusted-proxy／edge-policy／revocation／recovery security semantics 均未放寬。

最新已驗證 exact head `d7d3a02...` 的 PR workflow **18/18 completed，0 failure**，因此此 validation contract 漂移已收斂。

## 現行 gameplay truth

### 無職業角色

- fixed class 不再決定裝備／技能合法性。
- durable character state 不保存 `ClassID`。
- `CharacterRestore.LegacyRuntimeClassID`、`internal/classresource`、fixed-class resource lookup 已退役。
- generic action resource 由 `internal/actionresource` 擁有；production policy 由 `internal/actionpolicy` 擁有。
- legacy class wire 仍保留 compatibility contract，但不是 current gameplay truth。

### 20 技能／六格配置

- 20 個穩定 SkillID 已建立。
- authoritative learned-skill runtime state 已存在。
- 6 格 combat loadout 已存在，且非空配置必須是已學技能子集。
- learned skills／6 格 loadout 已 durable restore/save。
- 技能取得／學習 intent、忘卻／重置、六格何時可編輯仍沒有正式產品語義，Server 不自行發明。

### 六大角色屬性

正式 base stats：力量、敏捷、體質、智力、精神、魅力；中性基準皆為 10。

已進 live combat：

- effective AGI -> physical hit/evasion
- Strength -> melee production WeaponType basic-attack raw damage bonus
- effective AGI -> eligible damage critical chance

physical `basic-attack` 命中：

```text
HitChance = clamp(90% + (totalHit - totalEvasion) * 0.5 percentage points, 75%, 98%)
```

`totalHit` = attacker effective AGI hit modifier + weapon accuracy + authoritative unique-equipment hit。

`totalEvasion` = target effective AGI evasion modifier + authoritative unique-equipment evasion。

miss 不扣 HP，但仍 commit authoritative cooldown。

現行 melee production `one_hand_sword`、`one_hand_axe`、`mace` 的 basic-attack damage attribute 為 Strength。

eligible damage critical chance：base 5%；AGI 超過 30 後每完整 +10 AGI 增加 1pp，再加 `CriticalRating * 0.5pp`，上限 30%。

AGI ranged-damage seam 已存在，但 production 尚無正式 bow／crossbow／sling item archetype，不把 foundation 當成已完成玩家內容。

### 武器／裝備／unique item

- 5 把低階主手、3 面盾牌。
- MainHand／OffHand、裝備 durable restore/save 已存在。
- basic-attack cadence ownership：`WeaponType -> BasicAttackIntervalMS`。
- 18 類 V1 cadence 已 authored；空手 600ms。
- unique equipment 已有 stable `ItemInstanceID`、Server-rolled affix、exact-instance equip/unequip、durable state 與 v28 Type119–121。
- production catalog 仍只有既有 8 件低階裝備；新中／高階 item、掉率、商店、價格、任務、製作與經濟來源待產品決策。

### 皮膚 WeaponType affinity +1

- gameplay identity 使用 Server-owned `SkinID`。
- selected skin 與 authoritative main-hand WeaponType 相符時，目前 formal scope 的 `basic-attack` raw physical damage flat +1。
- 加值位置：weapon roll -> primary-stat bonus -> skin +1 -> unique flat physical -> critical -> defense/block/self mitigation。
- Client 透過 Type122 `AppearanceSnapshot.basic_attack_affinity_bonus` 讀 Server 算好的 0/1，不自行重算。
- 雙手劍現行 skin：`skin_heraklios_by_a_dizon`；舊 Dreyar ID fail-closed。
- Server 不保存 Mixamo UUID／GLB path／Client asset path。

### 遭遇／掉落／生命週期

- damage-driven threat V1 已存在。
- invalid target／despawn／defeat／leave／respawn／leash 有清理路徑。
- ground drop 有 authoritative bounded TTL。
- pickup／autopickup／expiry／rollback 共用 authoritative lifecycle。
- defeated character 不得 pickup／equip／unequip。
- generic transient-status lifecycle foundation 已存在。

## Client integration 狀態

### Client #21 — classless runtime retirement：完成／closed

Client 已回報 safe breaking boundary：

- current Client runtime 不再依賴 wire type 9／115／116／117 或 `wrong_class` 作產品語義。
- Client 可暫時保留 compatibility decoding。
- Type118 `CharacterTargetResourceState` 必須保持不變。
- Client validation run `34790382524` PASS。

因此 #21 不再是 audit／migration blocker。legacy class compatibility wire removal 延後到 v28 Client runtime 穩定後，另做獨立 Protocol cleanup；不預先占用下一版版本號。

### Client #22 — Type118 BrowserWS live path：完成／closed

Type118 live authority checkpoint 已完成，不再標記 NOT VALIDATED。

### Client #24 — v28 unique-equipment Type119–121：open

Server contract 已正式 handoff。等待 Client 啟用 codec／runtime store／UI／exact-instance intent 與 BrowserWS authority E2E。

Server 不為 Client staging 另建第二套 wire 或 optimistic item identity semantics。

### Client #25 — v28 AppearanceSnapshot Type122：open

等待 Client decode／store／presentation／BrowserWS live validation。

Client 只把 `SkinID` 映射到 presentation asset，不以本地 skin／weapon mapping 計算 gameplay bonus。

### Server #184 — skin WeaponType affinity +1：完成／closed

Server gameplay／persistence／Protocol contract 已完成；Client consumption 由 #25 追蹤。

## 地圖／場景狀態與工作邊界

依目前正式方向：**地圖由 Client 主導，Server 不先規劃。**

- map／scene layout、視覺動線、場景資產與 playable-space 由正式 Three.js Client 推進。
- Server 不主動排下一張地圖、新手村重做、castle／wall／portal／siege gate、blocker 數量或 spawn layout。
- Client 完成具體 playable slice 並需要 gameplay authority 時，再透過 Issue／comment handoff。
- Server 屆時只實作必要 authoritative collision／traversability／teleport／spawn／region／AI／siege legality。
- Client raycast／scene collider／node／asset path 不會自動成為 gameplay truth。

現有 `worlds/castle-sandbox/gameplay.json` 與 Emberwatch blockers 只視為現行既有 Server config／歷史相容事實，不代表未來地圖 roadmap。

## 近期 Server 工作順序

詳細拆分見 [待辦與待討論](待辦與待討論.md)。目前排序：

1. 依 [規劃總覽](規劃總覽.md) 的現行 roadmap 選下一個不需新增未定產品語義、且能增加玩家可見／可玩的 authoritative Server slice。
2. 等 Client #24／#25 消費 v28；有 concrete integration blocker 再回應，不先發明 alternate semantics。
3. 保持 Server CI、500-client load 與 production security E2E 作為 regression gates；出現 exact-head regression 才依 artifact 定位。
4. v28 Client runtime 穩定後，再規劃 legacy class wire 9／115／116／117 + `wrong_class` cleanup；Type118 不動。
5. character progression、創角配點、skill acquisition、loadout editing、未定 skill coefficients、正式中／高階裝備與經濟內容等，等待產品決策。
6. 地圖／場景由 Client 主導，Server 不排 map-first backlog；等 Client concrete handoff。

沒有新產品語義時，Server 可做 correctness／performance／validation debt，但不以 infrastructure-first 或 framework-first 工作填空。

## Active blocker

目前已驗證 code head `d7d3a02...` **沒有已確認的 Server correctness／capacity／production-validation blocker**。

Client #24／#25 是正式 consumer follow-up，不構成 Server correctness blocker；若 Client 回報 contract consumption 問題，再依正式 Protocol 處理。

## 驗證規則

不得宣稱 PASS，除非實際執行對應測試／CI。

最新已驗證 code head：

```text
d7d3a02fd94c0bc5e0a64c7ef5f4164d51602c1d
```

PR #179 的該 exact-head PR workflows：

```text
18/18 completed
0 failure
```

涵蓋：

- Server CI：PASS
- S3-E Scaling 500：PASS
- S3-E.8 Repeated Churn Soak：PASS
- S3-E.9 Long Mixed Gameplay Soak：PASS
- production TLS／trusted-proxy／edge-policy／revocation／recovery E2E：PASS

Server 基本 gate：

```text
go test ./...
go vet ./...
configured race detector
```

load／production E2E 是否 PASS 必須分別看對應 exact-head workflow；Server CI 綠燈不能替代 load／security E2E。

無法驗證時標記：**NOT VALIDATED**。
