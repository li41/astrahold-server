# 星壘 Astrahold — 專案狀態

本文件記錄 **目前** 的 Server 施工狀態、Protocol、整合 Issue、blocker 與驗證結果。長期方向見 [規劃總覽](規劃總覽.md)；下一步如何分類見 [待辦與待討論](待辦與待討論.md)。

歷史 milestone／recovery／舊 PR／舊 branch 只作證據，不在本文件重複堆疊完整流水帳。

## 目前基準

- 正式 Server：`li41/astrahold-server`
- gpt-server 唯一工作 branch：`ai-working-gpt-server`
- 正式 `main`：`75468425106965a2a0a86f8c6a56a1fdf282947b`
- 長期 PR／Actions 入口：PR #179
- 現行 Protocol：**v28**
- character schema：**v13**
- save journal schema：**v12**
- 最新已完整驗證 code checkpoint：`8cecace4285d552c8470961df49c859eb9d507c0`
- 該 exact head 的 PR workflow 最終狀態：**18/18 completed，0 failure**。
- 覆蓋 Server CI、S3-E Scaling 500、S3-E.8 Repeated Churn、S3-E.9 Long Mixed Gameplay Soak，以及 production TLS／trusted-proxy／revocation／recovery E2E。

其後若只有 `docs/**` 文件提交，即使 workflow 再次觸發或因 `paths-ignore` 未執行，也不能改寫 code checkpoint 的歸屬；本輪程式驗證明確歸屬 `8cecace4...`。

## 目前最重要的狀態

### 1. Protocol v28 gameplay contract — Server 完成

v28 正式啟用：

- Type119 `ClientEquipmentInstanceCommand`
- Type120 `InventoryInstanceSnapshot`
- Type121 `EquipmentInstanceSnapshot`
- Type122 `AppearanceSnapshot`

character-owner ReliableOrdered batch：

```text
InventorySnapshot
-> InventoryInstanceSnapshot (120)
-> EquipmentSnapshot
-> EquipmentInstanceSnapshot (121)
-> AppearanceSnapshot (122)
```

Type120／121 是 complete replacement snapshot；空集合也有意義，Client 必須能清 stale unique-instance state。

### 2. 500-client capacity gate 維持通過

最新已完整驗證 exact head `8cecace4...`：

- S3-E Scaling 500：PASS
- crowd-500：最初一次穩態 p99 `54.443189ms`，超過既有 `<50ms` hard gate；未修改程式、門檻或測試後，單次重跑穩態 p99 `46.193124ms`，PASS
- teleport-churn-500：PASS
- S3-E.8 Repeated Churn Soak：PASS
- S3-E.9 Long Mixed Gameplay Soak：PASS
- Server CI：PASS

第一次 crowd 超標的功能／網路／收斂條件均正常：500/500 ready、250000 spawn、decode/network error 皆為 0，收斂狀態完全 drain；因此保留為驗證觀察，不據此放寬 50ms gate，也不把第一次結果改寫成 PASS。最終同一 workflow 在未改 code 下單次重跑成功。

目前沒有已確認的 500-client gameplay／network／capacity blocker。50ms capacity gate 保持原樣；未來若再回歸，仍以 exact-head workflow 與 artifact 定位，不放寬 threshold。

### 3. 三階武器、三階盾牌與固定裝備屬性已進正式 Server catalog

正式 catalog revision：`weapon-progression-v1`。

目前：

- 17 種正式 `WeaponType` 都有低階、中階、高階武器原型。
- 低階 17 條代表武器線＋2 把額外單手劍；中階 17 把；高階 17 把，共 **53 把武器**。
- 三條盾牌方向都已有低階、中階、高階，共 **9 面盾牌**。
- catalog 共 **62 件裝備**。
- 中階武器／盾牌建立為唯一 `ItemInstanceID` 時 Server 產生 1 條合法詞綴；高階產生 2 條不同 `AffixID` 詞綴。
- `WeaponType` 的攻速、射程、物理普攻主屬性與持握需求跨階級共用，不允許單件中／高階武器私自覆寫。
- 雙手武器＋盾牌仍由 Server 雙向拒絕，且存檔／讀檔同樣拒絕非法組合。
- 中／高階目前沿用同一武器／盾牌線低階代表的重量與材質；未建立階級重量倍率。
- 釘棒固定 `ExtraDamage`：低階 +1、中階 +2、高階 +3。
- 法杖固定魔法威力：低階 +1、中階 +2、高階 +4。

盾牌三階正式數值：

| 方向 | 低階 | 中階 | 高階 |
| --- | --- | --- | --- |
| 均衡物防 | 物防 3／格擋 8%／格擋減傷 25%／魔法減傷 0% | 5／10%／30%／0% | 7／12%／35%／0% |
| 重格擋 | 4／10%／30%／0% | 6／13%／35%／0% | 8／16%／40%／0% |
| 抗魔 | 2／6%／20%／8% | 3／8%／25%／12% | 4／10%／30%／16% |

固定 archetype 屬性與隨機實例詞綴走同一權威裝備屬性聚合路徑；固定屬性不會被誤當成可重骰詞綴。裝備提供的力量、敏捷、體質、智力、精神、魅力會進入有效主屬性，而不是只存在於 `ItemInstance` 資料。

中／高階唯一實例不需要第二套武器／盾牌解析：`Inventory.MainHand()`／`Inventory.OffHand()` 會對 archetype 裝備與唯一實例都提供權威 `ItemArchetypeID`。武器再由 catalog 解析傷害、命中、`WeaponType` 攻速、射程與 skin affinity；盾牌再由 catalog 解析本體物防、格擋與額外魔法減傷。

高階抗魔盾已有整合測試保護：盾牌本體物防 4 可與唯一實例的物理防禦詞綴相加；魔法傷害先吃唯一實例魔法防禦，再獨立乘上盾牌 16% 額外魔法減傷。`equippedLowTierShield` 過時命名已改為 `equippedCatalogShield`，公式本身未改。

## 現行 gameplay truth

### 無職業角色

- fixed class 不再決定裝備／技能合法性。
- durable character state 不保存 `ClassID`。
- `CharacterRestore.LegacyRuntimeClassID`、`internal/classresource`、fixed-class resource lookup 已退役。
- generic action resource 由 `internal/actionresource` 擁有；production policy 由 `internal/actionpolicy` 擁有。
- legacy class wire 仍保留 compatibility contract，但不是 current gameplay truth。

### 20 技能／六格配置

- 20 個穩定 SkillID 已建立。
- authoritative learned-skill runtime state 已存在。
- 6 格 combat loadout 已存在，且非空配置必須是已學技能子集。
- learned skills／6 格 loadout 已 durable restore/save。
- 技能取得／學習 intent、忘卻／重置、六格何時可編輯仍沒有正式產品語義，Server 不自行發明。

### 六大角色屬性

正式 base stats：力量、敏捷、體質、智力、精神、魅力；中性基準皆為 10。

已進 live combat：

- effective AGI -> physical hit/evasion
- Strength -> melee production WeaponType basic-attack raw damage bonus
- AGI -> bow／crossbow／sling basic-attack raw damage bonus
- effective AGI -> eligible damage critical chance
- 裝備固定／隨機六大主屬性 -> effective primary stats

physical `basic-attack` 命中：

```text
HitChance = clamp(90% + (totalHit - totalEvasion) * 0.5 percentage points, 75%, 98%)
```

`totalHit` = attacker effective AGI hit modifier + weapon accuracy + authoritative equipment hit。

`totalEvasion` = target effective AGI evasion modifier + authoritative equipment evasion。

miss 不扣 HP，但仍 commit authoritative cooldown。

現行近戰正式武器的 physical basic-attack damage attribute 為 Strength；弓、十字弓、投石索為 Agility；法杖 V1 不自動取得物理主屬性加成。

eligible damage critical chance：base 5%；AGI 超過 30 後每完整 +10 AGI 增加 1pp，再加 `CriticalRating * 0.5pp`，上限 30%。

### 武器／裝備／unique item

- production catalog：53 把武器＋9 面盾牌，共 62 件裝備。
- MainHand／OffHand、裝備 durable restore/save 已存在。
- basic-attack cadence ownership：`WeaponType -> BasicAttackIntervalMS`。
- 18 類 V1 cadence 已 authored；空手 600ms。
- 投石索／弓／十字弓正式 ranged basic-attack range 分別為 14m／18m／22m。
- `WeaponType` 單手／雙手 gameplay 佔用已正式化；雙手與盾牌不可並存。
- unique equipment 已有 stable `ItemInstanceID`、Server-rolled affix、exact-instance equip/unequip、durable state 與 v28 Type119–121。
- 低階 0 詞綴、中階 1 詞綴、高階 2 條不同詞綴。
- 通用 `static_modifiers` 已存在；目前正式 weapon content 使用法杖固定魔法威力低 +1／中 +2／高 +4。
- 三階盾牌共用既有副手與唯一實例生命週期，不需要新增 Protocol 欄位。
- 掉率、商店、價格、任務、製作與經濟來源仍未定案；Server 不自行補完。

### 皮膚 WeaponType affinity +1

- gameplay identity 使用 Server-owned `SkinID`。
- selected skin 與 authoritative main-hand WeaponType 相符時，目前 formal scope 的 `basic-attack` raw physical damage flat +1。
- 加值位置：weapon roll -> primary-stat bonus -> skin +1 -> equipment flat physical -> critical -> defense/block/self mitigation。
- Client 透過 Type122 `AppearanceSnapshot.basic_attack_affinity_bonus` 讀 Server 算好的 0/1，不自行重算。
- Server 不保存 Mixamo UUID／GLB path／Client asset path。

### 遭遇／掉落／生命週期

- damage-driven threat V1 已存在。
- invalid target／despawn／defeat／leave／respawn／leash 有清理路徑。
- ground drop 有 authoritative bounded TTL。
- pickup／autopickup／expiry／rollback 共用 authoritative lifecycle。
- defeated character 不得 pickup／equip／unequip。
- generic transient-status lifecycle foundation 已存在。

## Client integration 狀態

### Client #21 — classless runtime retirement：完成／closed

Client current runtime 不再依賴 legacy class wire 作產品語義；Type118 `CharacterTargetResourceState` 維持正式 authority。

### Client #22 — Type118 BrowserWS live path：完成／closed

Type118 live authority checkpoint 已完成，不再標記 NOT VALIDATED。

### Client #24 — v28 unique-equipment Type119–121：open

Server contract 已正式 handoff。等待 Client 啟用 codec／runtime store／UI／exact-instance intent 與 BrowserWS authority E2E。

Server 不為 Client staging 另建第二套 wire 或 optimistic item identity semantics。

### Client #25 — v28 AppearanceSnapshot Type122：open

等待 Client decode／store／presentation／BrowserWS live validation。

Client 只把 `SkinID` 映射到 presentation asset，不以本地 skin／weapon mapping 計算 gameplay bonus。

## 地圖／場景狀態與工作邊界

依目前正式方向：**地圖由 Client 主導，Server 不先規劃。**

- map／scene layout、視覺動線、場景資產與 playable-space 由正式 Three.js Client 推進。
- Server 不主動排下一張地圖、新手村重做、castle／wall／portal／siege gate、blocker 數量或 spawn layout。
- Client 完成具體 playable slice 並需要 gameplay authority 時，再透過 Issue／comment handoff。
- Server 屆時只實作必要 authoritative collision／traversability／teleport／spawn／region／AI／siege legality。
- Client raycast／scene collider／node／asset path 不會自動成為 gameplay truth。

現有 `worlds/castle-sandbox/gameplay.json` 與 Emberwatch blockers 只視為現行既有 Server config／歷史相容事實，不代表未來地圖 roadmap。

## 近期 Server 工作順序

詳細拆分見 [待辦與待討論](待辦與待討論.md)。目前排序：

1. 武器與盾牌低／中／高三階原型、固定裝備屬性、雙手持握與唯一實例 consumption 已完成。
2. 下一個裝備切片是正式五個防具欄位與低階三類散裝；因目前 v28 裝備命令／快照只正式承載主手／副手，防具欄位必須先做正式 Protocol contract 與 Client handoff，不能把新欄位偷塞進 v28。
3. 防具欄位完成後，再沿用通用 `static_modifiers`、唯一實例詞綴與裝備屬性聚合做中／高階六套套裝；套裝效果應由 Server 依已裝備 `SetID` 數量推導，不持久化可漂移的 active bonus flag。
4. 等 Client #24／#25 消費 v28；有 concrete integration blocker 再回應，不先發明 alternate semantics。
5. 保持 Server CI、500-client load 與 production security E2E 作為 regression gates；出現 exact-head regression 才依 artifact 定位。
6. 裝備取得來源與經濟仍依已確認規劃逐步施工；掉率、價格、任務、製作等未定數值不得自行發明。
7. v28 Client runtime 穩定後，再規劃 legacy class wire 9／115／116／117 + `wrong_class` cleanup；Type118 不動。
8. character progression、創角配點、skill acquisition、loadout editing、未定 skill coefficients 等仍等待產品決策。
9. 地圖／場景由 Client 主導，Server 不排 map-first backlog；等 Client concrete handoff。

沒有新產品語義時，Server 可做 correctness／performance／validation debt，但不以 infrastructure-first 或 framework-first 工作填空。

## Active blocker

最新已完整驗證 code head `8cecace4...` **沒有已確認的 Server correctness／capacity／production-validation blocker**。

Client #24／#25 是正式 consumer follow-up，不構成 Server correctness blocker；若 Client 回報 contract consumption 問題，再依正式 Protocol 處理。

五個防具欄位尚未實作，原因不是 Server correctness blocker，而是需要新的正式 Protocol 裝備欄位契約與 Client handoff；在該 contract 建立前不以 v28 私下擴充。

## 驗證規則

不得宣稱 PASS，除非實際執行對應測試／CI。

最新已完整驗證 code head：

```text
8cecace4285d552c8470961df49c859eb9d507c0
```

PR #179 的該 exact-head PR workflows 最終狀態：

```text
18/18 completed
0 failure
```

涵蓋：

- Server CI：PASS（`go test ./...`、`go vet ./...`、configured race detector）
- S3-E Scaling 500：PASS（crowd-500、teleport-churn-500）
- S3-E.8 Repeated Churn Soak：PASS
- S3-E.9 Long Mixed Gameplay Soak：PASS
- production TLS／trusted-proxy／edge-policy／revocation／recovery E2E：PASS

本輪驗證歷史需保留：

- `781830c4...` 第一次 Server CI 因舊武器 progression 測試仍鎖 catalog 56 件而失敗；新盾牌整合測試本身已通過。只修正過時的總件數預期為 62，沒有放寬 gameplay 檢查。
- `8cecace4...` 第一次 crowd-500 穩態 p99 `54.443189ms` 超過 `<50ms` hard gate；功能／網路／收斂條件正常。未改程式、未改門檻後只重跑失敗工作一次，穩態 p99 `46.193124ms`，最終 Scaling workflow PASS。
- 50ms gate 維持不變；上述第一次失敗不得被描述為 PASS。

無法驗證時標記：**NOT VALIDATED**。
