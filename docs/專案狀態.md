# 星壘 Astrahold — 專案狀態

本文件記錄 **目前** 的 Server 施工狀態、Protocol、整合 Issue、blocker 與驗證結果。長期方向見 [規劃總覽](規劃總覽.md)；下一步分類見 [待辦與待討論](待辦與待討論.md)。

歷史 milestone／recovery／舊 PR／舊 branch 只作證據，不在本文件重複堆疊完整流水帳。

## 目前基準

- 正式 Server：`li41/astrahold-server`
- gpt-server 唯一工作 branch：`ai-working-gpt-server`
- 正式 `main`：`6fe9ec4e335d545a216a0534279dce2f849169dd`
- 現行 review／Actions 入口：Draft PR #194
- 已合併的歷史長期 PR：#179
- 現行 Protocol：**v34**
- character schema：**v15**
- save journal schema：**v12**
- gameplay world schema：**v4**
- 最新已驗證 code checkpoint：`ee10579d22b02d84a47e1de9b9129b711f7694eb`
- Server CI #1532 / run `35311171211`：**PASS**。

`ee10579d...` 已實際通過：

- `go test ./...`：PASS
- `go vet ./...`：PASS
- configured race detector：PASS

`ee10579d...` 是 Protocol v34 classless breaking-wire cleanup 的正式 Server checkpoint：移除 Type 9／115／116／117 定義與 codec support、退休 Type117 action-resource presentation、保留 Type118 target-resource authority與 retry path；歷史 persistence `ClassID` migration input仍保留。production trusted-proxy／TLS workflow source 的 readiness sentinel 已同步到 protocol=34，但本 checkpoint 沒有重新執行整套 production E2E，因此不把舊 v33 production E2E 結果冒充 v34 驗證。

## Protocol v34

v34 是 v33 之上的 classless breaking cleanup：

- 移除 Type 9 `ClientInitialClassSelection`。
- 移除 Type 115 `CharacterClassState`。
- 移除 Type 116 `InitialClassSelectionResult`。
- 移除 Type 117 `CharacterClassResourceState` 與 Server runtime publication lane。
- v34 codec 對 message IDs 9／115／116／117 回 unsupported；不建立 replacement class semantics。
- Type 118 `CharacterTargetResourceState` / `flaw` 保持 ID=118 與既有 Server->Client marshal-only contract，不新增 Client->Server intent。
- generic action-resource gameplay legality／mutation仍由 Server-owned character/action services決定，只是不再透過 retired Type117 做 class-named presentation。
- historical persistence `class_id` decode只留必要 migration boundary，不恢復 profession gameplay truth。

其餘 gameplay／warehouse／equipment／ammunition contract 完整繼承 v33。

### v33 inherited ammunition / warehouse

v33 延續 v32 warehouse 與既有 equipment contract，不重編 Type125–127；並新增 Server-authoritative ammunition selection，把 bow/crossbow 統一納入箭矢 requirement：

- Type 128 `ClientAmmunitionCommand`：Reliable `select` intent。
- Type 129 `AmmunitionResult`：correlated selected/rejected feedback。
- Type 130 `AmmunitionState`：authoritative manual selection；空值代表木箭 -> 銀箭 automatic。
- Type 125 `ClientWarehouseCommand`：Reliable warehouse intent。
- Type 126 `WarehouseResult`：以 `client_action_sequence` 對應 processed intent。
- Type 127 `WarehouseSnapshot`：完整 replacement snapshot。
- GM warehouse authorization 只信任 Server-authenticated subject `"1"`；Client 不傳 user/account/GM flag。
- map0 warehouse interaction authority 使用正式 `worlds/gm-room/gameplay.json` 的 `stock-keeper` blocker，不要求 `EntityNPC`。
- GM snapshot 兩種強化卷 `quantity=0` 代表 unlimited；Reliable replay 不得重複發物。
- personal warehouse 綁 CharacterID，current character schema v15 要求 initialized warehouse，durable restore 為最終 truth。

v30 裝備強化 contract 仍完整沿用：

- Type 3 `ClientEquipmentCommand`
- Type 111 `EquipmentSnapshot`
- Type119 `ClientEquipmentInstanceCommand`
- Type120 `InventoryInstanceSnapshot`
- Type121 `EquipmentInstanceSnapshot`
- Type122 `AppearanceSnapshot`
- Type123 `ClientEnhanceEquipment`
- Type124 `EquipmentEnhancementResult`

現行 Protocol v34 正式裝備欄沿用 v31 的 11 欄固定順序：

```text
main_hand
-> off_hand
-> helmet
-> chest
-> gloves
-> legs
-> boots
-> necklace
-> ring_1
-> ring_2
-> belt
```

Type111／121 對 formal equipment state 採 complete replacement；Type120／121 空集合同樣有清除 stale state 的語義。前七欄與 unique-instance identity 源自 [Protocol-v29-七欄裝備](Protocol-v29-七欄裝備.md)，四個 accessory 欄位由 v31 contract 擴充；現行 v34 不另建第二套 equipment truth。

Type123 只帶 `ScrollItemArchetypeID` 與 exact `ItemInstanceID` intent；Type124 回傳 `enhanced | nochange | broken | rejected`、rejection reason、previous/current enhancement level 與 `ScrollConsumed`。item instance snapshot 的 `enhancement_level` 是 Client 顯示 +N 的 authoritative truth。

弓／十字弓彈藥仍重用既有 inventory/action/combat truth，但 v33 新增 Type128–130 只負責 Server-authoritative 選箭 intent/result/state。

## 裝備／防具／箭矢／強化現況

production catalog 目前共 **107 個可裝備 equipment archetype**：

- 53 武器
- 9 盾牌
- 45 防具

107 不包含箭矢；箭是獨立的可堆疊背包消耗品，不新增第八個裝備欄位。

裝備主材質已收斂為 14 個 stable `MaterialID`；production 107 件以 exact `ItemArchetypeID` 鎖定單一 primary material，catalog validation 拒絕歷史複合／未知材質 ID。正式映射見 [裝備材質規劃](裝備材質規劃.md)。

目前唯一有 gameplay 效果的材質是 `silver`：當玩家 authoritative `main_hand` 是銀質武器、action 是 physical `basic-attack`、目標為 Server-owned `undead` 時，在 raw aggregate 後、critical 前套用 `floor(raw * 1.20)`。銀盾不提供該傷害加成，其他 13 種材質目前沒有隱性倍率。現行正式怪物尚未指定 `undead`，Server 不從名稱或 Client presentation 猜測分類。

### 裝備強化 V1

正式規則見 [裝備強化規劃](裝備強化規劃.md)，Server tracking Issue #199；Client handoff 為 `li41/astrahold-client-three.js#30`。

stable 卷軸：

- `item_astrahold_weapon_enhancement_scroll`：只可用於 weapon。
- `item_astrahold_armor_enhancement_scroll`：可用於 armor 與 shield。

安全值：

- 武器：+6。
- 防具／盾牌：+4。

安全值以下每次合法強化 100% 成功、固定 +1、消耗 1 張卷軸，不會無變化或爆裝。

武器從目前 +6 開始的成功／無變化／爆裝表：

- +6：60/20/20
- +7：50/25/25
- +8：40/30/30
- +9：30/35/35
- +10：20/40/40
- +11：10/45/45
- 目前 >= +12：固定 1/39/60

防具／盾牌從目前 +4 開始：

- +4：60/15/25
- +5：50/20/30
- +6：40/25/35
- +7：30/30/40
- +8：20/35/45
- +9：10/40/50
- 目前 >= +10：固定 1/39/60

**沒有 gameplay 最高強化上限。** 舊 `+10 at_limit` 已移除；尾段仍可用固定 1% 成功率繼續 +1。現在的 `uint16` enhancement level 只是 wire／storage 表示型別，不是 gameplay max；若未來接近表示邊界，應先做資料型別 migration，不能把型別上限變成遊戲規則。

合法嘗試的 Server outcome：

- `enhanced`：消耗 1 卷，保留同一 `ItemInstanceID`，`EnhancementLevel +1`。
- `nochange`：消耗 1 卷，裝備與等級不變。
- `broken`：消耗 1 卷並銷毀 exact target instance；若當時已裝備，同步清空該 equipment slot 並推進 equipment revision。
- `rejected`：不消耗卷軸。

Server 是 RNG 與 mutation owner；Client 不傳 roll／機率／預期結果，也不得 optimistic 扣卷、加 +N 或刪爆裝。可靠 action sequence 在 mutation 前檢查並只接受一次，避免 replay 重複處理。

目前 +N combat stat effect：

- 武器 `+N` -> equipped `PhysicalDamage +N`
- 防具／盾牌 `+N` -> equipped `PhysicalDefense +N`

### 弓箭彈藥 V1

正式規則見 [箭矢規劃](箭矢規劃.md)。

- 正式 ammo：`item_low_arrow` 木箭、`item_silver_arrow` 銀箭。
- bow 與 crossbow 都需要箭；成立射擊含 miss 都消耗 exactly 1。
- automatic：木箭 -> 銀箭。
- v33 Type128–130 提供雙擊選箭的 Reliable intent、result、authoritative state。
- 手動 selected stack 存在時 bow/crossbow 優先使用；耗盡後回 automatic。
- selection V1 session-scoped；reconnect 回 automatic，不提升 character schema。
- bow 與 crossbow 都使用 weapon-base + actual-arrow 的單次 combined damage roll；bow 本體為 2–3／3–4／4–5，加箭後為 8–11／9–12／10–13；crossbow 本體為 6–7／11–13／16–19，加箭後仍為 12–15／17–21／22–27。
- fired silver arrow 對 Server-classified undead 套 shared +20% raw physical basic-attack rule；銀主手＋銀箭最多一次。

v33 最新 Server balance checkpoint `34fea259...` / CI #1491：**PASS**。

45 件防具分成：

- 15 低階散裝：0 affix、無 set、無 base-stat requirement。
- 15 中階三套：unique `ItemInstanceID`、每件 1 條 Server-rolled affix。
- 15 高階三套：unique `ItemInstanceID`、每件 2 條不同 Server-rolled affix。

六套正式 armor set：

- `set_garrison_steel` 衛戍鋼甲
- `set_windchaser_huntgear` 逐風獵裝
- `set_arcane_rune_robes` 秘紋法衣
- `set_starforged_bastion` 星鑄壁壘
- `set_starshadow_huntgear` 星影追獵
- `set_starlight_robes` 星輝法衣

高階需求：

- 星鑄壁壘：durable base Constitution >= 18
- 星影追獵：durable base Agility >= 18
- 星輝法衣：durable base Intelligence >= 18

requirement 只看角色永久 base stats，不計裝備、affix、set bonus、passive skill、status 等 derived/effective bonus。live equip 與 durable restore 都由 Server 驗證；裝備自己的屬性不能反向滿足自己的穿戴門檻。

套裝 2/5 件 activation 從目前 authoritative equipped pieces 即時計算，不持久化第二份 mutable set-active state。fixed modifiers、unique affixes、set bonuses 共用 authoritative equipment stat aggregation。

unique 裝備 archetype 與 exact instance 都會透過正式 owner snapshots 複寫 Client state；Client 不得依 UI index 或 optimistic local equipment state 決定 gameplay。

## 持久化

Server durable inventory 已使用通用 slot encoding 保存七欄 archetype／unique-instance equipment。

unique `ItemInstance.EnhancementLevel` 會走既有 durable instance persistence；成功強化保存新 +N，nochange 不改等級，broken 後 exact instance 不應在後續 durable snapshot／reconnect 還原。

背包 stack 的 durable snapshot 可保存箭矢 stable `ItemArchetypeID` 與數量；射擊扣箭修改正式 inventory truth，並透過既有 inventory replication／persistence 路徑保存。

歷史 `MainHand`／`OffHand`／hand-instance persistence 欄位只作讀取相容；canonicalization 會遷移到通用 equipment encoding。新存檔不以手部專用欄位作 current truth。

character restore 會在 world mutation 前驗證 identity、world identity、角色核心狀態、inventory canonical shape、裝備 legality 與 applicable base-stat requirements；非法 restore fail closed，不以自動脫裝或 derived stat 補足門檻。

## 現行 gameplay truth

### 無職業角色／技能

- fixed class 不再決定裝備／技能合法性。
- durable character state 不保存 `ClassID`。
- generic action resource 由 `internal/actionresource` 擁有；production policy 由 `internal/actionpolicy` 擁有。
- 20 個 stable SkillID、authoritative learned-skill state、6 格 combat loadout 與 durable persistence 已存在。
- 技能取得／學習、忘卻／重置、六格編輯產品語義仍未定；Server 不自行發明。

### 六大角色屬性／戰鬥

正式 base stats：Strength、Agility、Constitution、Intelligence、Spirit、Charisma；中性基準皆為 10。

已進 live combat：

- effective AGI -> physical hit/evasion
- Strength -> melee production WeaponType basic-attack raw damage bonus
- Agility -> bow／crossbow／sling basic-attack raw damage bonus
- bow basic-attack -> authoritative arrow requirement／consumption＋bow damage＋arrow damage
- crossbow basic-attack -> authoritative arrow requirement／consumption＋crossbow base damage＋arrow damage
- effective AGI -> eligible critical chance
- equipment fixed/affix/set primary stats -> effective primary stats
- weapon enhancement -> equipped PhysicalDamage
- armor/shield enhancement -> equipped PhysicalDefense
- silver main-hand 或實際 fired silver arrow 的 physical basic-attack -> undead raw damage +20%（floor，位於 critical 前；最多套一次）

命中、damage、critical、defense、block、material interaction、箭矢消耗、裝備強化與 skin affinity 最終結果皆由 Server 決定。

### 遭遇／掉落／生命週期

- damage-driven threat V1 已存在。
- invalid target／despawn／defeat／leave／respawn／leash 有 cleanup path。
- ground drop 有 authoritative bounded TTL。
- pickup／autopickup／expiry／rollback 共用 authoritative lifecycle。
- defeated character 不得 pickup／equip／unequip／強化裝備。
- generic transient-status lifecycle foundation 已存在。

## 地圖 authority

Client 仍主導 layout／visual scene／assets；Server 只承接已 handoff 的 gameplay facts。

目前 Server 已納入：

- 第一大陸 authoritative movement envelope：X `-2600..2600`、Z `-900..4100`。
- 12 個地表 Region core polygon。
- 9 個地下 map authority entry；有精確 envelope 的地下空間保存正式 envelope，其餘只保存 Client 已定義的 authority metadata，不捏造未定 geometry。

Client terrain tile、render padding、scene node、asset path、visual collider 不會自動成為 Server gameplay truth。

## Map0 GM 密室倉庫 / 奧德里克

Server v32 live path 已完成並由 Server CI #1470 驗證：

- BrowserWS Type125 可經 gateway -> ownership fence -> authoritative worldruntime。
- loopback E2E trusted bootstrap 可由 Server 注入 AuthenticationSubject；Client gameplay payload 無法選 GM 身分。
- live E2E 直接載入正式 `worlds/gm-room/gameplay.json`，interaction range 從 `stock-keeper` blocker 推導。
- subject `"1"` 的 `open_gm` / `withdraw_gm`、非 `"1"` 的 `not_authorized`、一般角色 `open_personal` / `deposit_personal` / `withdraw_personal` 都已經過真 BrowserWS Type125 -> Type126/127 round-trip。
- Reliable replay live 驗證：先領 2、重送同 sequence、再新 sequence 領 1，authoritative InventorySnapshot 最終為 3，不是 5。

Server #202（舊 BrowserWS MapID blocker）已用 CI #1461 PASS 證據關閉。

Client #29 已以 current-v33 inherited warehouse path 完成真實 BrowserWS runtime acceptance 並關閉。驗收包含 map0 `stock-keeper` 奧德里克玩家可見呈現、精確台詞／兩個選項、`open_gm`／`open_personal`、Type126 correlation、Type127 complete replacement、GM／non-GM authority、personal deposit/withdraw 與 reconnect authoritative restore；Client 未新增 gameplay authority。

## Client integration

### Client #36 — Map1 Loot V2 presentation handoff：open

已建立 `[gpt-server]` Issue #36：Server `07c60110...` / CI `35347470862` PASS，Protocol 維持 v34。Client 需確認 `item_gold_coin`、`item_minor_speed_potion`、灰狼皮、箭矢、卷軸、low-tier 與既有 TierMid exact-instance 的 inventory／ground presentation；ground stack quantity 與 exact instance payload 保持 Server-private，Client 不從 EntitySpawn 猜 quantity、不重擲 affix、不 optimistic pickup mutation。小型加速藥水目前只有 loot identity／source，效果語義仍未定案。

### Client #35 — Map1 monster base combat identity / presentation handoff：open

已建立 `[gpt-server]` Issue #35 通知 Client：Server code checkpoint `7c401574...`／CI `35340474912` PASS，Protocol 維持 v34、沒有新 wire field；playtest monster identity 由 `wolf-gray-01` cutover 為正式 `monster_gray_wolf`，ordinary melee action 由 `wolf-bite` 收斂為 shared `monster-melee`。Client 只需確認 stable ArchetypeID 的灰狼 presentation、generic melee presentation 與既有 EntitySpawn／ActionStarted／CombatEvent／Vitals／Despawn consumption，不得本地推導 damage、hit、cooldown、aggro 或 assist。特殊攻擊／VFX contract 明確延後，不在 #35 偷渡。

### Client #34 — Protocol v34 classless breaking wire：completed

Client candidate `0b6fbf1e1e8c064887d0a3370ef9578ba8c24f0e` 已完成 active v34 cutover 並關閉 #34。`src/protocol/index.ts` 現 export v34；current `MESSAGE_TYPE`／`ClientIntent`／`ServerMessage` 已排除 9／115／116／117，`wrong_class` 不再屬 current rejection vocabulary；Type118 保持 ID=118 與既有 authoritative consumption。client-ci run `35312797404` PASS，dedicated v34 live job `105498292274` PASS，真 BrowserWS 已驗證 v34 SessionWelcome、Type118 `flaw` build→clear、target despawn 後 store clear，且 raw observation 中 9／115／116／117 未出現。

### Client #29 — GM 密室奧德里克倉庫：completed

Server checkpoint `7da0d741...` / CI #1470 已 PASS；Client current-v33 live candidate `c7f4a3a7...` 亦已完成 dedicated BrowserWS runtime acceptance 並關閉 #29。正式 authority 仍是 Type125 intent -> Server validate/decide -> Type126/127 + authoritative inventory state；Client 只呈現，不做 optimistic inventory／warehouse mutation。

### Client #30 — Protocol v30 裝備強化：completed

已建立 `[gpt-server]` handoff：Type123／124、`enhancement_level`、兩種 stable scroll ID、武器安全 +6、防具／盾牌安全 +4、完整成功／無變化／爆裝機率表、沒有 gameplay 最大強化值，以及 Client 禁止 optimistic 扣卷／加值／刪裝。Server checkpoint `bb8e58a...` 已通過 Server CI #1389。

### Client #27 — Protocol v33 弓／十字弓彈藥：completed

Client main `848b1a72df5aedb2911b7dcbbf3df15bbaf29102` 已完成 Type128–130、雙擊選箭、bow／crossbow authoritative ammo HUD、selected depletion -> auto、reconnect auto 與 live runtime acceptance；Client issue #27 已關閉。Client 不本地扣箭、不重算 shootability／cooldown／silver-undead bonus，也不本地重算 weapon+arrow damage，只呈現 Server `InventorySnapshot`／`AmmunitionState`／`CombatEvent`。

Server 後續 balance-only checkpoint `34fea259...` / CI #1491 PASS 將 bow 本體收斂為 2–3／3–4／4–5，木／銀箭仍為 6–8，因此 bow 最終三階為 8–11／9–12／10–13；crossbow 規則與 v33 wire 不變。這項數值調整不需要 Client 新 message 或重新實作。

### Client #26 — 七欄＋45 件防具：completed

Client current-v33 已確認十一欄 authoritative equipment UI 的前七欄、完整 45 件 armor identity、代表性中／高階 exact instance 與 Type111/121 snapshot consumption。先前 live acceptance 發現的 Server #203 gateway slot fence 已修正並關閉。

production gateway checkpoint `4f93b3ef...` / Server CI #1502 **PASS**：gateway 現以 `protocol.EquipmentSlots()` 作 Type3／119 ingress vocabulary，全部 formal slots 可進既有 worldruntime validation；unknown slot 仍 fail closed。該 equipment contract 由 v34 原樣繼承，item/slot legality、exact-instance ownership、base-stat requirement、hand conflict 等 gameplay rule 仍只由 authoritative worldruntime 決定。

最新 `fd458b11...` / Server CI #1508 **PASS** 的真 BrowserWS authority coverage 已包含：Type3 low chest round-trip、Type119 exact mid chest、wrong-slot／missing／高階 Constitution requirement rejection no-mutation、exact unequip、current-schema join restore 的 Type111/120/121 replacement state / affix preservation，以及 production `item_two_hand_iron_sword`／`item_iron_rim_round_shield` 的雙向 hand-conflict rejection no-mutation。

Client #26 已完成 current-v33 最後 runtime acceptance 並關閉。Client candidate `f810f7f0...` 的 client-ci run `35309579844` 與 dedicated BrowserWS live job `105488859854` 均 PASS；驗收涵蓋 45 件 armor presentation identity、low Type3／mid exact Type119 操作、wrong-slot／requirement／missing／hand-conflict rejection no-optimistic mutation、Server affix 原樣消費，以及第二個真 BrowserWS session 的 Type111/121 exact armor relogin restore。MaterialID／銀對不死 V1 沒有新增 wire field。

Client source 對 gpt-server 維持只讀。

### Client #24 — unique-equipment Type119–121：completed

current-v33 inherited path 已完成 BrowserWS runtime acceptance 並關閉 #24：Type120/121 replacement snapshot、exact `ItemInstanceID`、Server affix／`enhancement_level`、Type119 exact-instance equip/unequip 與 production UI presentation 均已驗證；Client 沒有 optimistic mutation。

### Client #25 — AppearanceSnapshot Type122：completed

Client current-v33 已完成 Type122 decode/store、玩家可見 selected `SkinID`／authoritative affinity on-off presentation，以及同一 durable selected skin 的 disconnect/relogin first-owner-batch restore 驗收並關閉 #25。Client UI 只格式化 Server `basic_attack_affinity_bonus`，沒有建立本地 skin-to-WeaponType gameplay truth。

### Client #21／#22／#24／#25／#26／#27／#29／#34：完成

classless runtime retirement 與 Type118 BrowserWS authority checkpoint 已完成；Server-side legacy class wire cleanup 亦已在 v34 完成。

## 目前 blocker／待決策

目前沒有已確認的 equipment／material／arrow／enhancement gameplay correctness blocker，也沒有已確認的 Client protocol integration blocker。Server v34 `ee10579d...` / CI #1532 與 Client `0b6fbf1e...` / client-ci `35312797404` / dedicated live job `105498292274` 已完成互通驗收；Type118 build/clear 與 retired 9／115／116／117 absence 均有真 BrowserWS 證據。

CI maintenance：

- production trusted-proxy／TLS workflow source 的 Protocol sentinel 已更新為 v34；本次只由 Server CI #1532 驗證 Go source，不把尚未重跑的 production E2E 標成 v34 PASS。

仍屬產品決策而非 correctness bug：

- 玩家基礎生命／魔力 authored V1 已定：`BaseMaxHP = 15 + 11 × (Level - 1)`、`BaseMaxMP = 6 + 2 × (Level - 1)`，再疊體質／精神 derived bonus 與正式裝備／效果修正；數值尺度參考 `li41/myriad-throne-server`，但不搬職業模板、隨機升級或職業倍率。現行 runtime 仍使用 1000 HP／100 MP playtest baseline，尚未 migration。
- Map1 普通近戰 monster foundation 已在 Server `7c401574b182f31e3965e48eaeb88aca985922d6` 完成並由 Server CI run `35340474912` **PASS**（Test／Vet／race detector）：10 種 stable archetype 的 authored stats 已進 `internal/monstercatalog`；runtime 已消費 `DamageMin..DamageMax`、PhysicalHit／Evasion、CriticalRating、physical／magic defense，防禦曲線已由舊 `+20` migration 到 `defense/(defense+100)`；AggroMode、passive retaliation、same-family one-hop assist、threat／leash／lifecycle seam 已落地。playtest 灰狼已由 `wolf-gray-01`／`wolf-bite` cutover 為 `monster_gray_wolf`／shared `monster-melee`。其餘 9 種的 exact spawn/home/patrol/encounter placement 尚未上線。正式 Server runtime 目前仍只有 `worlds/castle-sandbox` 與 `worlds/gm-room` gameplay proxy，但 Map1 半成品並非不存在：`li41/astrahold-assets` 的 `ai-working-claude@d2b4c6f...` 已有 `assets/environment/terrain/map1/runtime/terrain.json`、`land.bin`、`collision.json`、layout／surface／scatter；Client `BakedRegion` 亦已支援 `?region=map1`。其中 `collision.json` 明確自述為 Server proposal，不是 Client gameplay truth。Server `94dbe4726cbc1d4187cccc60b310061b86e5397d` 已加入通用 f32le heightfield loader、SHA 驗證、bilinear sampling 與 navigation surface height overlay，CI run `35350154037` **PASS**（Test／Vet／race detector）。下一步是把這份 candidate bake 的真 LFS `land.bin` 與 accepted collision subset 正式納入 Server world bundle，再在 authoritative terrain 上落 spawn；不再從 presentation-only POI 猜座標。BaseXP 仍只是 metadata，XP owner 未實作；特殊攻擊延後另行規劃。
- Map1 Loot Catalog V2 已在 Server `07c60110ab1d6d94e176bc0148c965bd9b6a514e` 完成，Server CI run `35347470862` **PASS**（Test／Vet／race detector）：10 種 monster exact table 已進 `internal/map1loot`；stack drop 支援 `QuantityMin..QuantityMax`，`item_gold_coin` 為 zero-weight stack；TierMid `equipment_instance` 在 loot resolution 時建立 exact ItemInstanceID 並只 roll 一次 affix，auto-loot／public pickup 保留同一 instance；private ground payload 不新增 Protocol，v34 不變。playtest 灰狼已使用正式 Map1 loot table。尚未驗證的是完整 Map1 spawn 後的 gold/hour、gear/hour、inventory pressure 等 economy balance。
- 哪些正式 monster archetype 屬於 `undead`。
- character level／屬性點 mutation contract。
- 技能取得／學習與未定技能數值。
- skin selection 正式玩家 mutation 入口。
- 未來祝福／詛咒強化卷軸 stable identity 與其 Astrahold 正式產品語義。
- 未來特殊箭與是否將選箭 preference 做 durable persistence。

Server 不自行填補上述 gameplay semantics。

## 驗證規則

- 沒有實際跑對應 tests／CI，不宣稱 PASS。
- expensive production E2E 只在 meaningful checkpoint 執行。
- code checkpoint 與 docs-only checkpoint 分開記錄，不用文件 commit 冒充 code validation。
