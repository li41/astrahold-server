# 武器攻速、普攻距離與負重規則

本文件記錄 Astrahold 正式 Server 的武器基本攻擊與裝備負重規則。Server 是唯一 gameplay authority；Client 只依 authoritative state/events 呈現動畫，不得以動畫、本地計時、raycast、asset mapping 或本地裝備資料決定攻擊是否合法、距離、傷害、攻速或負重結果。

## 權威規則

- 持械普攻的基礎間隔由 `WeaponType -> BasicAttackIntervalMS` 持有。
- 同一 `WeaponType` 的所有 `ItemArchetypeID` 共用同一基礎攻速；單件武器不得覆寫。
- 遠程普攻距離由 `WeaponType -> BasicAttackRange` 持有；單件武器與階級不得自行延長距離。
- 沒有正式距離覆寫的武器沿用 `basic-attack` action definition 的 `4.5` 公尺。V1 只有投石索、弓、十字弓覆寫普攻距離。
- 空手沒有裝備 `WeaponType`，使用 `basic-attack` action definition 的 `600 ms` 基礎間隔與 `4.5` 公尺距離。
- Server 在 action prepare/commit path 決定實際 cooldown 與 range legality；Client 不得以本地 raycast 決定命中是否合法。
- 盾牌不產生 basic attack；釣竿是道具，不列入武器攻速。
- 未來若有裝備、Buff 或屬性攻速加成，仍由 Server 算出 effective cadence；不得要求 Client 自行套倍率決定 gameplay timing。

## 正式 V1 武器基本攻擊規則

| 分類 | `WeaponType` | 基礎間隔 | 普攻距離 | 物理普攻傷害主屬性 |
| --- | --- | ---: | ---: | --- |
| 空手 | 無（`basic-attack` fallback） | 600 ms | 4.5 m | 無武器型別加成 |
| 指虎 | `knuckles` | 600 ms | 4.5 m | 力量 |
| 短刀／匕首 | `dagger` | 650 ms | 4.5 m | 力量 |
| 鉤爪 | `claw` | 700 ms | 4.5 m | 力量 |
| 雙刀 | `dual_blades` | 700 ms | 4.5 m | 力量 |
| 單手劍 | `one_hand_sword` | 900 ms | 4.5 m | 力量 |
| 單手矛 | `one_hand_spear` | 1000 ms | 4.5 m | 力量 |
| 單手斧 | `one_hand_axe` | 1050 ms | 4.5 m | 力量 |
| 釘棒 | `mace` | 1050 ms | 4.5 m | 力量 |
| 投石索 | `sling` | 1100 ms | **14 m** | 敏捷 |
| 戰錘 | `warhammer` | 1150 ms | 4.5 m | 力量 |
| 星錘 | `morning_star` | 1200 ms | 4.5 m | 力量 |
| 弓 | `bow` | 1200 ms | **18 m** | 敏捷 |
| 法杖 | `staff` | 1250 ms | 4.5 m | 暫無物理主屬性加成 |
| 雙手矛 | `two_hand_spear` | 1300 ms | 4.5 m | 力量 |
| 雙手劍 | `two_hand_sword` | 1350 ms | 4.5 m | 力量 |
| 雙手斧／戟 | `two_hand_axe` | 1500 ms | 4.5 m | 力量 |
| 十字弓 | `crossbow` | 1550 ms | **22 m** | 敏捷 |

### 力量與敏捷邊界

- 所有近戰武器的物理基本攻擊傷害統一吃力量，包括匕首、指虎、鉤爪與雙刀。
- 弓、十字弓、投石索的物理基本攻擊傷害吃敏捷。
- 敏捷仍負責既有的物理命中、閃避、遠程物理傷害與高敏捷暴擊成長；不再同時提供近戰武器傷害。
- 法杖 V1 不因 `WeaponType` 自動取得力量、敏捷或智力的物理基本攻擊傷害加成。

## 正式 V1 負重規則

- 每個角色的最大攜帶重量固定為 **100**。
- 物品重量由 Server 的 `ItemArchetypeID` 資料持有，必須是正整數。
- 背包內物品與已裝備物品都計入同一個總負重；裝備／卸下不會改變總負重。
- 拾取、加入背包或交換後若會超過 100，Server 原子拒絕該操作；不會先吞掉地上掉落再回滾。
- 移除、丟棄或消耗物品後會立即釋放對應負重。
- 隨機詞綴與 `ItemInstanceID` 不改變基礎重量。
- V1 不由力量、職業、等級或裝備效果提高負重上限。
- V1 沒有輕載／重載／超重分段，也沒有超重後降低移速的狀態；到達上限後直接拒絕新增重量。
- 重量不影響移動速度、攻擊間隔、施法速度、傷害、命中、閃避、冷卻、屬性或動畫速度。

## 不做裝備耐久度

- Astrahold V1 不建立裝備耐久值、磨損、破損或修理系統。
- 攻擊、受擊、死亡、裝備與卸下都不降低任何耐久值。
- 不建立修理費、修理 NPC、耐久協定欄位或預留的耐久 gameplay state。
- 程式中的 `durable...` 命名若出現，是指「可持久化保存的狀態」，不是物品磨損耐久度。

## 實作位置

- `internal/equipmentcatalog/default.json`：`WeaponType` 正式基礎間隔、物理普攻傷害主屬性與遠程普攻距離。
- `config/combat-actions.json`：空手與沒有距離覆寫的 `basic-attack` 基礎規則，目前正式值為 `600 ms`、`4.5 m`。
- `internal/equipmentcatalog/catalog.go`：共用 `WeaponType` 規則載入、驗證與查詢。
- `internal/worldruntime/weapon_basic_attack.go`：持械普攻攻速與傷害套用。
- `internal/worldruntime/weapon_basic_attack_range.go`：持械普攻距離覆寫，發生在 Server 距離合法性判定前。
- `internal/worldruntime/inventory.go`：角色最大攜帶重量 `100` 與正式物品重量來源。
- `internal/inventory/inventory.go`：負重上限的原子加入／移除／交換規則。
- `internal/equipmentcatalog/catalog_test.go`、`internal/inventory/weight_test.go`、`internal/worldruntime/weapon_basic_attack_integration_test.go`：規則測試保護。

## 呈現邊界

Client 的動畫實測可作為商用品質與 presentation constraint 的輸入，但不能反向成為 gameplay authority。若動畫片段短於 Server 間隔，可播完後回待機等待；若動畫需要裁切或調整播放速率，Client 可以處理呈現，但不得延後 Server 已合法的下一次攻擊。遠程武器的準星、射線、拋射物與特效只負責呈現，不能自行放寬或縮短 Server 的合法攻擊距離。
